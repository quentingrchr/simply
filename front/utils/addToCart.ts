// * todo add reducer with dispatch for addToCart
// export const addToCart = (product: any) => {
//   setCart((cart: ICarteState): ICarteState => {
//     let newCart
//     if (!cart) {
//       newCart = { items: [product] }
//     } else {
//       newCart = { ...cart, items: [...cart.items, product] }
//     }
//     return newCart
//   })
// }

export const addToCart = () => {}
